<!DOCTYPE html>
<html lang="en">
  <head>
    <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <link
        rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
      />
      <link rel="stylesheet" href="/stylesheets/decks-index.css" />
      <title>Document</title>
    </head>
  </head>
  <body>
    <header>
      <nav>
        <a href="/"
          ><span class="material-symbols-outlined"> arrow_back_ios </span
          ><span class="material-symbols-outlined"> empty_dashboard </span></a
        >
        <h1>Edit</h1>

        <a href="/"><span class="material-symbols-outlined"> logout </span></a>
      </nav>
    </header>
    <h2><%=deckName%></h2>
    <!-- delete button here  -->
    <form
      action="/decks/<%=foundDeck._id%>/flashcards/<%=cardID%>?_method=DELETE"
      method="POST"
    >
      <button type="submit">
        <span class="material-symbols-outlined"> delete </span>
      </button>
    </form>

    <!-- preview -->
    <!-- preview flashcard selected in display view-->
    <!-- update form here -->
    <div class="preview-container">
      <section>
        <form
          action="/decks/<%=foundDeck._id%>/flashcards/<%=cardID%>?_method=PUT"
          method="POST"
        >
          <strong> Preview </strong>
          <div class="front">
            <div class="fc-header"><strong>Front</strong></div>
            <input type="text" name="frontEdit" value="<%=fc.front%>" />
          </div>
          <div class="back">
            <div class="fc-header"><strong> Back </strong></div>
            <input type="text" name="backEdit" value="<%=fc.back%>" />
          </div>
          <div class="tags">
            <label>Tags: </label>
            <input type="text" name="tagEdit" value="<%=fc.tags%>" />
          </div>
          <button type="submit">Save Changes</button>
        </form>
      </section>
    </div>
  </body>
</html>
